# 允許存取上傳的檔案
Options -Indexes

# 安全設定 - 只允許特定檔案類型
<FilesMatch "\.(jpg|jpeg|png|gif|webp|mp4|avi|mov|wmv)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# 禁止執行 PHP 檔案
<FilesMatch "\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>